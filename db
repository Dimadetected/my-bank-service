CREATE TABLE accounts(
    id INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE,
    currency text,
    sum real not null DEFAULT 0
);

CREATE TABLE payments(
    id INTEGER PRIMARY KEY AUTOINCREMENT UNIQUE,
    sum REAL NOT NULL,
    is_checked BOOLEAN DEFAULT FALSE,
    type TEXT not null);

INSERT INTO accounts (currency, sum) VALUES ('SBP', 0);